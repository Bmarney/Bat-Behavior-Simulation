using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TemporaryRoosts : MonoBehaviour
{
    // Start is called before the first frame update

    // public TemporaryRoosts tempRoost;
    //public Vector3 locate = tempRoost.transform.position;

    void Start()
    {
       
    }

    public Vector3 roostLocation()
    {
        return transform.position;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
